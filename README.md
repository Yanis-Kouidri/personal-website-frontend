# Portfolio frontend

Create with Vite + React.

## Development

Do:

    git switch dev

then

    cp .env.example .env

Then setup env variable in `.env`  
After, do:

    yarn

then

    yarn dev

## Production
